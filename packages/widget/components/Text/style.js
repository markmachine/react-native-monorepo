import {StyleSheet} from 'react-native';
import {BLACK} from '../../colors';

export default StyleSheet.create({
  container: {
    fontStyle: 'normal',
    fontWeight: '400',
    color: BLACK,
    fontSize: 14,
  },
});
